import xyz.ronella.gradle.plugin.simple.dotnet.core.task.*
plugins {
  id "xyz.ronella.dotnet.core" version "2.0.1"
}

tasks.register('clearGlobal') {
	group = 'manage'
	dependsOn(
		'Records:clear',
		'ConnectionTCP:clear',
		'ExecutionStrategies:clear'
	)
}